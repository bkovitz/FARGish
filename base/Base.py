# Base.py -- Base types, classes, and functions for FARGish
#
# These are collected into one file because they refer to each other,
# hence would cause circular imports if separated until multiple files.

