Solve pons asinorum

    Want

        QuerySlipnetForDelegate

            QInput  DONE

                BeforeFromAvails  DONE

                AfterFrom  DONE

            QPred, SearchFor  DONE

            Make the QuerySlipnetForDelegate codelet

                pulse_slipnet

                build the delegate if successful

        adjust support

    Detector(15)

        LookForAvail

        RaiseException

        Call Detectors from FARGModel


    Slipnet of Consumers

    do_timestep()

    maybe

        Merge slipnet results (to get _ + _ = _)



Other things

    Match type of codelet_args; throw a Snag if can't match.

    Query slipnet to resolve snags.

    Overriding/varying Agent arguments.

Small things

    Give the() a generic return type.
